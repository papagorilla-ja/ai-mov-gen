server {
    listen       80;
    server_name  localhost;
    root         /usr/share/nginx/html;
    index        index.html;
    client_max_body_size 100M;

    # Vue SPA: 全パスを index.html にフォールバック
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # API リバースプロキシ: /api/* → api コンテナ (port 8000)
    location /api/ {
        proxy_pass         http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # WebSocket プロキシ: /ws/* → api コンテナ
    location /ws/ {
        proxy_pass         http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
        proxy_read_timeout 3600s;
    }

    # /projects/ 静的ファイルプロキシ
    location ^~ /projects/ {
        proxy_pass         http://api:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
    }

    # /static/ 静的ファイルプロキシ (アバター・サムネイルなど)
    location ^~ /static/ {
        proxy_pass         http://api:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
    }

    # /template-assets/ 静的ファイルプロキシ
    #   レイアウト選択ギャラリーのサムネイルは iframe 内で実物のスライドを描くため、
    #   同梱フォントと chart.min.js をブラウザから取得できる必要がある。
    #   ^~ を付けるのは必須。付けないと下の正規表現ロケーションが .js を先に拾い、
    #   nginx がローカルを探して 404 になる（グラフだけ描かれないサムネイルになる）。
    location ^~ /template-assets/ {
        proxy_pass         http://api:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
    }

    # 静的ファイルのキャッシュ設定
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ヘルスチェック用エンドポイント
    location /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
